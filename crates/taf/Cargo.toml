[package]
name = "taf"
version.workspace = true
description = "The Tonie Audio Format: header codec, Ogg framing and packet padding, no_std and dependency-free"
keywords = ["taf", "toniebox", "teddycloud", "ogg", "opus"]
categories = ["encoding", "multimedia::encoding", "parser-implementations", "no-std"]
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["std"]
std = ["alloc"]
alloc = []

[dev-dependencies]
sha1 = { workspace = true }

[lints]
workspace = true
