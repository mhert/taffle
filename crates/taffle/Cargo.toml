[package]
name = "taffle"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# The SHA-1 a TAF's audio region is hashed with as it is read back: `taf` states what it needs from
# one and takes the implementation from whoever uses it, and this is that implementation for a host.
sha1 = { workspace = true }
taf = { workspace = true }
taf-encode = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
